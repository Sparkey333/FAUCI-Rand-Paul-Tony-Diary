## What this changes

<!-- One or two sentences. What is different after this merges? -->

## Why

<!-- The problem being solved. Link an issue if there is one. -->

## How it was verified

<!-- What you actually ran, and what it said. Not what you expect to pass. -->

- [ ] `cargo test --workspace`
- [ ] `cargo fmt --all --check` and `cargo clippy --all-targets`
- [ ] `provenance ledger check` (if the ledger changed)
- [ ] Desktop app built and launched (if `desktop/` changed)

## Ledger entries

<!-- Delete this section if no ledger entries were added or changed.

     Every new or modified claim needs a citation. A claim with no source must
     stay `unverified`. List the sources here so a reviewer can check them
     without reading the JSON. -->

## Notes for the reviewer

<!-- Anything unresolved, deliberately out of scope, or worth a second opinion. -->
