{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window: core APIs plus the native folder picker.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
